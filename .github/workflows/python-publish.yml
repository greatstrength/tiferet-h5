name: Build and Publish tiferet-h5

on:
  push:
    tags:
      - 'v*a*'     # alpha pre-releases: v0.1.0a1, v0.1.0a2, etc.
  release:
    types: [published]  # beta pre-releases and stable releases

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10"]
        # Explicit tiferet-version coverage for the pyproject.toml pin
        # (currently `tiferet>=2.0.3,<2.1`). 2.0.3 is the *only* released
        # version inside that range as of this workflow (verified against
        # PyPI) -- add more entries here as tiferet ships 2.0.4+ or a 2.1
        # prerelease before beta freeze, so the range is never wider than
        # what CI has actually exercised.
        tiferet-version: ["2.0.3"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install .[test]
          pip install "tiferet==${{ matrix.tiferet-version }}"

      - name: Run tests
        run: |
          pytest --verbose

  build:
    runs-on: ubuntu-latest
    needs: test
    env:
      TIFERET_H5_SILENT_IMPORTS: "1"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install build tools
        run: |
          python -m pip install --upgrade pip
          pip install build twine setuptools>=42 wheel

      - name: Extract version from tiferet_h5/__init__.py
        id: get_version
        run: |
          VERSION=$(python -c "from tiferet_h5 import __version__; print(__version__)")
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Validate version matches tag
        run: |
          TAG=${{ github.ref_name }}
          PACKAGE_VERSION=${{ steps.get_version.outputs.version }}
          if [ "$TAG" != "v$PACKAGE_VERSION" ]; then
            echo "Error: Tag ($TAG) does not match package version (v$PACKAGE_VERSION)"
            exit 1
          fi

      - name: Build package
        run: |
          python -m build

      - name: Check package
        run: |
          twine check dist/*

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: package-dist
          path: dist/

  publish:
    runs-on: ubuntu-latest
    needs: build
    environment: pypi

    permissions:
      id-token: write  # Required for OIDC authentication with PyPI

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: package-dist
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
